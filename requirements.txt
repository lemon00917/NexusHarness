# 核心依赖（必装）
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-anthropic>=0.1.0   # PROVIDER=anthropic 时使用
typing_extensions>=4.0.0

# OpenAI 兼容 provider 依赖
langchain-openai>=0.1.0

# 前端 Web 界面
fastapi>=0.100.0
uvicorn>=0.23.0
sse-starlette>=1.8.0

# 数据库连接（openGauss / PostgreSQL）
psycopg2-binary>=2.9.0