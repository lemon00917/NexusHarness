# Artifacts

This directory stores historical manual test outputs, screenshots, and legacy
root-level test scripts that are not part of the runtime source tree.

- `manual-runs/`: ad-hoc API/UI run outputs and reports.
- `legacy-root-tests/`: old root-level test scripts and reports kept for
  reference. They are not part of the current primary test layout.
